import { ReactNode } from 'react';

export const ResetPasswordPage = async (): Promise<ReactNode> => (
  <>
    <h1 className='text-primary mb-6 text-4xl font-semibold'>Réinitialiser le mot de passe</h1>
  </>
);
